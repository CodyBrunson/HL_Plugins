{
  "name": "hl_plugins",
  "author": "Tomb",
  "license": "LGPL-3.0-or-later",
  "private": true,
  "workspaces": [
    "*"
  ],
  "scripts": {
    "build-all": "yarn workspaces foreach --all run build && shx rm -rf dist && shx mkdir dist && shx cp -r \"./*/dist/*\" dist"
  },
  "packageManager": "yarn@4.9.2",
  "devDependencies": {
    "rimraf": "^6.0.1"
  },
  "dependencies": {
    "@highlite/core": "^2.1.1"
  }
}
